name: xwendngakan_app
description: "EduBook - IQ — Xwendngakan"
publish_to: 'none'
version: 1.1.0+27

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

  # State Management
  provider: ^6.1.2

  # Navigation
  go_router: ^13.2.1

  # HTTP & Networking
  http: ^1.2.0

  # Storage
  shared_preferences: ^2.2.3

  # UI
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  lottie: ^3.1.0
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  animate_do: ^3.3.4
  smooth_page_indicator: ^1.1.0
  flutter_staggered_animations: ^1.1.1
  gap: ^3.0.1
  flutter_rating_bar: ^4.0.1
  image_picker: ^1.0.7
  percent_indicator: ^4.2.3

  # Utils
  url_launcher: ^6.2.6
  share_plus: ^12.0.2
  connectivity_plus: ^6.0.3
  youtube_player_flutter: ^9.1.1

  # Maps
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  google_maps_flutter: ^2.9.0
  table_calendar: ^3.2.0
  firebase_core: ^4.7.0
  firebase_messaging: ^16.2.0
  firebase_auth: ^6.1.0
  flutter_local_notifications: ^18.0.1
  package_info_plus: ^9.0.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.14.2
  flutter_native_splash: ^2.4.7

# Pin path_provider_foundation to the last release before it switched to the
# `objective_c` FFI package. objective_c 9.3.0 ships a framework whose arm64
# slice references the simulator platform, which the App Store rejects
# (validation error 91169 — "unsupported platform in the arm64 slice").
dependency_overrides:
  path_provider_foundation: 2.4.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_logo.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  web:
    generate: false

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/banners/
    - assets/icons/
    - assets/animations/

  fonts:
    - family: Rabar
      fonts:
        - asset: assets/fonts/Rabar-Regular.ttf
          weight: 400
        - asset: assets/fonts/Rabar-Medium.ttf
          weight: 500
        - asset: assets/fonts/Rabar-Bold.ttf
          weight: 700

flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/app_logo.png"
  android_12:
    image: "assets/images/app_logo.png"
    color: "#FFFFFF"
